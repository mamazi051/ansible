---
  - name: install nginx 
    package:
      name: "{{packages}}"
      state: present

  - name: copy nginx config
    template:
      src: site.conf.j2
      dest: /etc/nginx/conf.d/mysitee.conf

  
